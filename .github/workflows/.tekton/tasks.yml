apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: test-cleanup
spec:
  steps:
    - name: run-tests
      image: python:3.10
      script: |
        pip install pytest
        pytest

    - name: cleanup
      image: alpine
      script: |
        echo "Cleaning workspace..."
        rm -rf /workspace/*
